# Langfuse .NET SDK Environment Configuration
# Copy this file to .env and fill in your values
# .env files are ignored by git

# ===========================================
# Cloud Integration Tests (Langfuse Cloud)
# ===========================================

# Required: Get these from your Langfuse project settings
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key

# Langfuse instance URL (defaults to EU cloud)
LANGFUSE_BASE_URL=https://cloud.langfuse.com
# LANGFUSE_BASE_URL=https://us.cloud.langfuse.com  # US region

# Optional: Name of test prompt (create this in Langfuse UI first)
LANGFUSE_TEST_PROMPT_NAME=test-prompt

# ===========================================
# Local Integration Tests (Docker)
# ===========================================

# Local Langfuse instance URL
LANGFUSE_LOCAL_URL=http://localhost:3003

# Local API keys (get from your local Langfuse instance)
LANGFUSE_LOCAL_PUBLIC_KEY=pk-lf-local
LANGFUSE_LOCAL_SECRET_KEY=sk-lf-local
